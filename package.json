{
  "name": "noiceboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A noice embeddable GitHub profile dashboard for repo number 69.",
  "scripts": {
    "generate": "node scripts/generate-noiceboard.mjs",
    "generate:demo": "NOICEBOARD_DEMO=1 node scripts/generate-noiceboard.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": [
    "github-profile",
    "readme",
    "svg",
    "dashboard",
    "github-pages"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
